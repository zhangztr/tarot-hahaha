# ToS / Privacy / Blog Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add Terms of Service, Privacy Policy, and Blog pages with Footer, bilingual i18n support, and markdown rendering.

**Architecture:** Four new page components (TosPage, PrivacyPage, BlogListPage, BlogPostPage) added to HashRouter. Legal pages render i18n markdown strings via `react-markdown`. Blog reads a manifest.json index and loads per-locale .md files via `import.meta.glob`. Footer placed in Layout, visible on all pages.

**Tech Stack:** React 19, Vite 6, TypeScript 5.7 (strict), Tailwind CSS 4, react-router-dom v7, react-markdown.

## Global Constraints

- `noUnusedLocals: true`, `noUnusedParameters: true` — no dead code
- All user-facing strings go through i18n (en.ts defines `TranslationKey` type, zh.ts implements)
- Follow existing patterns: `useT()` hook for translations, Tailwind utility classes, `motion` for animations
- HashRouter, base path `/`
- No new external dependencies beyond `react-markdown`

---

### Task 1: Install react-markdown

**Files:**
- Modify: `package.json`

**Interfaces:**
- Produces: `react-markdown` available for import as `ReactMarkdown` (default export)

- [ ] **Step 1: Install the package**

Run: `npm install react-markdown`

- [ ] **Step 2: Verify install**

Run: `npm ls react-markdown`
Expected: `tarot-web@1.0.0 ... └── react-markdown@X.Y.Z`

- [ ] **Step 3: Commit**

```bash
git add package.json package-lock.json
git commit -m "chore: add react-markdown dependency"
```

---

### Task 2: Add i18n keys for all new pages and footer

**Files:**
- Modify: `src/i18n/en.ts`
- Modify: `src/i18n/zh.ts`

**Interfaces:**
- Produces: New `TranslationKey` entries consumed by Footer, TosPage, PrivacyPage, BlogListPage, BlogPostPage

- [ ] **Step 1: Add keys to en.ts (TranslationKey source of truth)**

Add after the existing "theme.cardSynergy" line (before `} as const;`):

```typescript
  // Footer
  "footer.tos": "Terms of Service",
  "footer.privacy": "Privacy Policy",
  "footer.blog": "Blog",
  "footer.copyright": "© 2026 Tarot Web. All rights reserved.",

  // ToS Page
  "tos.title": "Terms of Service",
  "tos.content": `**Last updated: July 24, 2026**

## 1. Acceptance of Terms

By accessing and using Tarot Web ("the Service"), you agree to be bound by these Terms of Service. If you do not agree, please discontinue use of the Service.

## 2. Description of Service

Tarot Web is an online tarot card reading application provided for entertainment purposes only. The readings, interpretations, and content generated by the Service do not constitute professional advice (medical, legal, financial, or psychological).

## 3. User Conduct

You agree to use the Service only for lawful purposes and in a manner that does not infringe upon the rights of others.

## 4. Intellectual Property

All content, design, and code of the Service are the property of Tarot Web unless otherwise stated. The tarot card interpretations and original content are protected by copyright.

## 5. Disclaimer of Warranties

The Service is provided "as is" without warranties of any kind, express or implied. We do not guarantee the accuracy, completeness, or reliability of any readings or content.

## 6. Limitation of Liability

Tarot Web shall not be liable for any damages arising from the use or inability to use the Service, including but not limited to decisions made based on readings.

## 7. Changes to Terms

We reserve the right to modify these terms at any time. Continued use of the Service after changes constitutes acceptance of the new terms.

## 8. Contact

For questions about these terms: ztry666@qq.com`,

  // Privacy Page
  "privacy.title": "Privacy Policy",
  "privacy.content": `**Last updated: July 24, 2026**

## 1. Information We Collect

Tarot Web does not require user registration and does not knowingly collect personally identifiable information. We may collect non-personal usage data through third-party services (see Section 3).

## 2. Cookies

The Service uses cookies for essential functionality (such as remembering your language preference) and for third-party advertising. By using the Service, you consent to the use of cookies.

## 3. Third-Party Services

### Google AdSense

We display advertisements via Google AdSense, which may use cookies and web beacons to serve personalized ads based on your browsing history. Google's use of advertising cookies enables it and its partners to serve ads based on your visit to this site and/or other sites on the Internet.

You may opt out of personalized advertising by visiting [Google Ads Settings](https://www.google.com/settings/ads) or by using the [Network Advertising Initiative opt-out page](https://optout.networkadvertising.org/).

### Analytics

We may use analytics services to collect anonymized usage data (page views, visit duration, browser type) to improve the Service.

## 4. Data Storage

Language preference is stored locally in your browser via localStorage. No personal data is stored on our servers.

## 5. Children's Privacy

The Service is not directed to children under the age of 13. We do not knowingly collect personal information from children.

## 6. Your Rights

Since we do not collect personal identifiable information, there is no personal data to access, modify, or delete. For questions about this policy or your data, contact us at ztry666@qq.com.

## 7. Changes to This Policy

We may update this Privacy Policy from time to time. Changes will be posted on this page with an updated revision date.

## 8. Contact

For privacy-related inquiries: ztry666@qq.com`,

  // Blog
  "blog.title": "Blog",
  "blog.backToList": "← Back to Blog",
  "blog.noPosts": "No articles yet. Check back soon!",
  "blog.publishedOn": "Published on",
```

- [ ] **Step 2: Add keys to zh.ts**

Add after the existing "theme.cardSynergy" line (before `};`):

```typescript
  // Footer
  "footer.tos": "服务条款",
  "footer.privacy": "隐私政策",
  "footer.blog": "博客",
  "footer.copyright": "© 2026 塔罗秘境. 保留所有权利。",

  // ToS Page
  "tos.title": "服务条款",
  "tos.content": `**最后更新：2026年7月24日**

## 1. 接受条款

访问和使用塔罗秘境（以下简称"本服务"），即表示您同意遵守本服务条款。如不同意，请停止使用。

## 2. 服务说明

塔罗秘境是一个在线塔罗牌抽牌应用，仅供娱乐参考。本服务生成的解读和内容不构成任何形式的专业建议（包括医疗、法律、财务或心理建议）。

## 3. 用户行为规范

您同意仅将本服务用于合法目的，且不得侵犯他人权利。

## 4. 知识产权

本服务的所有内容、设计和代码，除非另有声明，均归塔罗秘境所有。塔罗卡牌解读和原创内容受版权保护。

## 5. 免责声明

本服务按"现状"提供，不作任何明示或暗示的保证。我们不保证任何解读或内容的准确性、完整性或可靠性。

## 6. 责任限制

塔罗秘境不对因使用或无法使用本服务而产生的任何损害承担责任，包括但不限于基于解读所作出的决定。

## 7. 条款变更

我们保留随时修改本条款的权利。修改后继续使用本服务即表示接受新条款。

## 8. 联系方式

如有任何关于本条款的问题：ztry666@qq.com`,

  // Privacy Page
  "privacy.title": "隐私政策",
  "privacy.content": `**最后更新：2026年7月24日**

## 1. 我们收集哪些信息

塔罗秘境无需用户注册，不会主动收集个人身份信息。我们可能通过第三方服务收集非个人的使用数据（见第3条）。

## 2. Cookie 使用

本服务使用 Cookie 来实现基本功能（如记住您的语言偏好）和第三方广告。使用本服务即表示您同意使用 Cookie。

## 3. 第三方服务

### Google AdSense

我们通过 Google AdSense 展示广告，该服务可能使用 Cookie 和网络信标根据您的浏览历史投放个性化广告。Google 对广告 Cookie 的使用使其及其合作伙伴能够根据您访问本网站和/或互联网上其他网站的情况投放广告。

您可以通过访问 [Google 广告设置](https://www.google.com/settings/ads) 或使用 [Network Advertising Initiative 退出页面](https://optout.networkadvertising.org/) 选择退出个性化广告。

### 统计分析

我们可能使用统计分析服务收集匿名使用数据（页面浏览量、访问时长、浏览器类型）以改进本服务。

## 4. 数据存储

语言偏好通过 localStorage 存储在您的浏览器本地。我们没有服务器，不存储任何个人数据。

## 5. 儿童隐私

本服务不面向 13 岁以下的儿童。我们不会故意收集儿童的个人信息。

## 6. 您的权利

由于我们不收集个人身份信息，因此没有可访问、修改或删除的个人数据。如有关于本政策或数据的问题，请联系 ztry666@qq.com。

## 7. 政策变更

我们可能会不时更新本隐私政策。变更内容将发布在本页面并更新修订日期。

## 8. 联系方式

如有隐私相关问题：ztry666@qq.com`,

  // Blog
  "blog.title": "博客",
  "blog.backToList": "← 返回博客列表",
  "blog.noPosts": "暂无文章，敬请期待！",
  "blog.publishedOn": "发布于",
```

- [ ] **Step 3: Verify TypeScript compiles**

Run: `npx tsc -b --noEmit`
Expected: No errors (new keys are added to both en and zh, TranslationKey is derived from en, so zh must match)

- [ ] **Step 4: Commit**

```bash
git add src/i18n/en.ts src/i18n/zh.ts
git commit -m "feat: add ToS, Privacy, Blog, and Footer i18n keys"
```

---

### Task 3: Create blog data files (manifest + first article)

**Files:**
- Create: `src/data/blog/manifest.json`
- Create: `src/data/blog/getting-started-en.md`
- Create: `src/data/blog/getting-started-zh.md`

**Interfaces:**
- Produces:
  - `manifest.json` → imported by BlogListPage and BlogPostPage
  - `.md` files → loaded by BlogPostPage via `import.meta.glob`

- [ ] **Step 1: Create manifest.json**

```json
[
  {
    "slug": "getting-started",
    "date": "2026-07-24",
    "zh": {
      "title": "塔罗新手入门指南",
      "summary": "了解塔罗牌的基础知识、常见牌阵以及如何使用本网站进行你的第一次抽牌。"
    },
    "en": {
      "title": "Tarot Beginner's Guide",
      "summary": "Learn the basics of tarot cards, common spreads, and how to do your first reading on this site."
    }
  }
]
```

- [ ] **Step 2: Create getting-started-en.md**

```markdown
# Tarot Beginner's Guide

Welcome to Tarot Web! Whether you're completely new to tarot or looking for a refresher, this guide will walk you through everything you need to know.

## What Is Tarot?

Tarot is a deck of 78 cards, each carrying symbolic imagery and meaning. The deck is divided into two main groups:

- **Major Arcana** (22 cards): These represent significant life events, spiritual lessons, and archetypal forces. Cards like *The Fool*, *The Lovers*, and *Death* deal with the big themes of the human experience.
- **Minor Arcana** (56 cards): These cover everyday situations and are divided into four suits — Cups (emotions), Wands (action), Swords (intellect), and Pentacles (material world).

## How to Use This Site

1. **Think of a question** — It can be about love, career, personal growth, or simply "What should I focus on today?" You can also leave it blank and let the cards offer general guidance.
2. **Choose a spread** — We offer three options:
   - *Single Card*: Quick insight into the present moment.
   - *Past · Present · Future*: See how past events shape the present and what lies ahead.
   - *Problem · Obstacle · Advice*: Identify the core issue and find a path forward.
3. **Draw your cards** — Click the button and watch the shuffle animation. Your cards will be revealed one by one.
4. **Read the interpretation** — Each card comes with its meaning in the context of its position in the spread.

## A Few Tips

- You don't need to believe in anything mystical to use tarot. Many people treat it as a tool for self-reflection — the cards help you think about your situation from a different angle.
- There are no "bad" cards. Even cards like *Death* or *The Tower* represent transformation and necessary change, not literal doom.
- Reversed cards (upside-down) often point to blocked energy, internal challenges, or an alternative side of the card's meaning.

## Ready to Begin?

Head back to the home page, ask your question, and draw your first card!
```

- [ ] **Step 3: Create getting-started-zh.md**

```markdown
# 塔罗新手入门指南

欢迎来到塔罗秘境！无论你是完全的新手还是想复习基础知识，这篇指南将带你了解所需的一切。

## 什么是塔罗牌？

塔罗牌共有 78 张，每张牌都有其独特的象征意义和画面。整套牌分为两大类：

- **大阿尔卡纳**（22 张）：代表重大人生事件、精神课题和原型力量。像*愚人*、*恋人*、*死神*这样的牌涉及人生中的大主题。
- **小阿尔卡纳**（56 张）：涵盖日常生活中的各种情况，分为四个牌组——圣杯（情感）、权杖（行动）、宝剑（理智）和钱币（物质）。

## 如何使用本站

1. **想一个问题**——可以是关于爱情、事业、个人成长，或者简单地"今天应该关注什么？"也可以什么都不填，让牌给出一般性的指引。
2. **选择一个牌阵**——我们提供三种选择：
   - *单张牌*：当下处境的快速指引。
   - *过去·现在·未来*：了解过去如何影响现在，以及未来可能的走向。
   - *问题·阻碍·建议*：识别核心问题，找到前进方向。
3. **抽牌**——点击按钮，观看洗牌动画。你的牌会一张一张亮出来。
4. **阅读解读**——每张牌在牌阵的特定位置上都有对应的含义解读。

## 一些小建议

- 你不需要相信任何神秘主义的东西也可以使用塔罗。很多人把它当作一种自我反思的工具——牌能帮助你从不同角度思考自己的处境。
- 没有"坏牌"。即使是*死神*或*高塔*这样的牌也代表转变和必要的改变，而不是字面意义上的厄运。
- 逆位的牌（倒过来的牌）通常指向受阻的能量、内心的挑战，或牌义的另一种面向。

## 准备开始了吗？

回到首页，提出你的问题，抽你的第一张牌吧！
```

- [ ] **Step 4: Verify JSON is valid and TypeScript can resolve module**

Run: `npx tsc -b --noEmit`
Expected: No errors (JSON module will be resolved by `resolveJsonModule` which is part of `moduleResolution: "bundler"`)

- [ ] **Step 5: Commit**

```bash
git add src/data/blog/
git commit -m "feat: add blog manifest and first article (getting-started)"
```

---

### Task 4: Create Footer component

**Files:**
- Create: `src/components/Footer.tsx`
- Modify: `src/components/Layout.tsx` — add Footer import and render

**Interfaces:**
- Consumes: `"footer.tos"`, `"footer.privacy"`, `"footer.blog"`, `"footer.copyright"` from i18n
- Produces: `<Footer />` component rendered in Layout

- [ ] **Step 1: Create Footer.tsx**

```typescript
import { Link } from "react-router-dom";
import { useT } from "../hooks/useT";

export default function Footer() {
  const t = useT();

  return (
    <footer className="w-full border-t border-white/10 bg-black/20">
      <div className="max-w-2xl mx-auto px-6 py-6 flex flex-col sm:flex-row items-center justify-between gap-3">
        <span className="text-white/30 text-xs">
          {t("footer.copyright")}
        </span>
        <nav className="flex items-center gap-4 text-xs text-white/40">
          <Link to="/blog" className="hover:text-white/70 transition-colors">
            {t("footer.blog")}
          </Link>
          <span className="text-white/20">|</span>
          <Link to="/tos" className="hover:text-white/70 transition-colors">
            {t("footer.tos")}
          </Link>
          <span className="text-white/20">|</span>
          <Link to="/privacy" className="hover:text-white/70 transition-colors">
            {t("footer.privacy")}
          </Link>
        </nav>
      </div>
    </footer>
  );
}
```

- [ ] **Step 2: Update Layout.tsx to include Footer**

Add the import:
```typescript
import Footer from "./Footer";
```

Add `<Footer />` after `</main>` and before `</div>` (the inner z-index div):

```typescript
import type { ReactNode } from "react";
import Header from "./Header";
import Footer from "./Footer";
import StarfieldBackground from "./StarfieldBackground";

export default function Layout({ children }: { children: ReactNode }) {
  return (
    <div className="min-h-screen flex flex-col bg-mystic-deep relative">
      <StarfieldBackground />
      <div className="relative z-[1] flex flex-col flex-1">
        <Header />
        <main className="flex-1 flex flex-col items-center px-6 pb-24">
          {children}
        </main>
        <Footer />
      </div>
    </div>
  );
}
```

- [ ] **Step 3: Verify TypeScript compiles**

Run: `npx tsc -b --noEmit`
Expected: No errors

- [ ] **Step 4: Commit**

```bash
git add src/components/Footer.tsx src/components/Layout.tsx
git commit -m "feat: add Footer component with ToS/Privacy/Blog links"
```

---

### Task 5: Create TosPage and PrivacyPage

**Files:**
- Create: `src/pages/TosPage.tsx`
- Create: `src/pages/PrivacyPage.tsx`

**Interfaces:**
- Consumes: `"tos.title"`, `"tos.content"`, `"privacy.title"`, `"privacy.content"` from i18n
- Consumes: `ReactMarkdown` from `react-markdown`

- [ ] **Step 1: Create TosPage.tsx**

```typescript
import ReactMarkdown from "react-markdown";
import { useT } from "../hooks/useT";

export default function TosPage() {
  const t = useT();

  return (
    <div className="flex-1 w-full max-w-2xl pt-8">
      <h1 className="font-serif text-2xl text-white mb-6">{t("tos.title")}</h1>
      <div className="bg-white/[0.03] border border-white/10 rounded-xl p-6 sm:p-8">
        <div className="prose prose-invert prose-sm max-w-none
          prose-headings:text-white/90 prose-headings:font-serif prose-headings:mt-6 prose-headings:mb-3
          prose-p:text-white/60 prose-p:leading-relaxed
          prose-a:text-mystic-gold/80 prose-a:no-underline hover:prose-a:text-mystic-gold
          prose-strong:text-white/80
          prose-li:text-white/60
          [&_h2]:text-lg [&_h2]:border-b [&_h2]:border-white/10 [&_h2]:pb-2">
          <ReactMarkdown>{t("tos.content")}</ReactMarkdown>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Create PrivacyPage.tsx**

```typescript
import ReactMarkdown from "react-markdown";
import { useT } from "../hooks/useT";

export default function PrivacyPage() {
  const t = useT();

  return (
    <div className="flex-1 w-full max-w-2xl pt-8">
      <h1 className="font-serif text-2xl text-white mb-6">{t("privacy.title")}</h1>
      <div className="bg-white/[0.03] border border-white/10 rounded-xl p-6 sm:p-8">
        <div className="prose prose-invert prose-sm max-w-none
          prose-headings:text-white/90 prose-headings:font-serif prose-headings:mt-6 prose-headings:mb-3
          prose-p:text-white/60 prose-p:leading-relaxed
          prose-a:text-mystic-gold/80 prose-a:no-underline hover:prose-a:text-mystic-gold
          prose-strong:text-white/80
          prose-li:text-white/60
          [&_h2]:text-lg [&_h2]:border-b [&_h2]:border-white/10 [&_h2]:pb-2">
          <ReactMarkdown>{t("privacy.content")}</ReactMarkdown>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: Verify TypeScript compiles**

Run: `npx tsc -b --noEmit`
Expected: No errors

- [ ] **Step 4: Commit**

```bash
git add src/pages/TosPage.tsx src/pages/PrivacyPage.tsx
git commit -m "feat: add ToS and Privacy Policy pages"
```

---

### Task 6: Create BlogListPage

**Files:**
- Create: `src/pages/BlogListPage.tsx`

**Interfaces:**
- Consumes: `manifest.json` (import)
- Consumes: `"blog.title"`, `"blog.noPosts"`, `"blog.publishedOn"` from i18n
- Produces: Renders at route `/blog`

- [ ] **Step 1: Create BlogListPage.tsx**

```typescript
import { Link } from "react-router-dom";
import { useLocale } from "../hooks/useT";
import { useT } from "../hooks/useT";
import blogManifest from "../data/blog/manifest.json";

interface BlogEntry {
  slug: string;
  date: string;
  zh: { title: string; summary: string };
  en: { title: string; summary: string };
}

const entries = blogManifest as BlogEntry[];

const sorted = [...entries].sort(
  (a, b) => new Date(b.date).getTime() - new Date(a.date).getTime()
);

export default function BlogListPage() {
  const { locale } = useLocale();
  const t = useT();

  return (
    <div className="flex-1 w-full max-w-2xl pt-8">
      <h1 className="font-serif text-2xl text-white mb-6">{t("blog.title")}</h1>

      {sorted.length === 0 ? (
        <p className="text-white/40 text-sm">{t("blog.noPosts")}</p>
      ) : (
        <div className="flex flex-col gap-4">
          {sorted.map((entry) => {
            const meta = locale === "zh" ? entry.zh : entry.en;
            const dateStr = new Date(entry.date).toLocaleDateString(
              locale === "zh" ? "zh-CN" : "en-US",
              { year: "numeric", month: "long", day: "numeric" }
            );

            return (
              <Link
                key={entry.slug}
                to={`/blog/${entry.slug}`}
                className="block bg-white/[0.03] border border-white/10 rounded-xl p-5
                  hover:bg-white/[0.06] hover:border-white/20 transition-colors group"
              >
                <h2 className="font-serif text-lg text-white/90 group-hover:text-white transition-colors mb-2">
                  {meta.title}
                </h2>
                <p className="text-white/45 text-sm leading-relaxed mb-3 line-clamp-2">
                  {meta.summary}
                </p>
                <time className="text-white/25 text-xs">
                  {t("blog.publishedOn")} {dateStr}
                </time>
              </Link>
            );
          })}
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 2: Verify TypeScript compiles**

Run: `npx tsc -b --noEmit`
Expected: No errors

- [ ] **Step 3: Commit**

```bash
git add src/pages/BlogListPage.tsx
git commit -m "feat: add Blog list page with manifest-driven entries"
```

---

### Task 7: Create BlogPostPage

**Files:**
- Create: `src/pages/BlogPostPage.tsx`

**Interfaces:**
- Consumes: `manifest.json` (import)
- Consumes: `.md` files via `import.meta.glob` with `?raw` query
- Consumes: `"blog.backToList"`, `"blog.publishedOn"` from i18n
- Consumes: `ReactMarkdown` from `react-markdown`

- [ ] **Step 1: Create BlogPostPage.tsx**

```typescript
import { useParams, Link } from "react-router-dom";
import { useEffect, useState } from "react";
import ReactMarkdown from "react-markdown";
import { useLocale, useT } from "../hooks/useT";
import blogManifest from "../data/blog/manifest.json";

interface BlogEntry {
  slug: string;
  date: string;
  zh: { title: string; summary: string };
  en: { title: string; summary: string };
}

const entries = blogManifest as BlogEntry[];

const mdModules = import.meta.glob<{ default: string }>(
  "../data/blog/*.md",
  { query: "?raw", import: "default" }
);

export default function BlogPostPage() {
  const { slug } = useParams<{ slug: string }>();
  const { locale } = useLocale();
  const t = useT();

  const [content, setContent] = useState<string | null>(null);

  const entry = entries.find((e) => e.slug === slug);

  useEffect(() => {
    if (!entry) return;
    const loader = mdModules[`../data/blog/${entry.slug}-${locale}.md`];
    if (!loader) {
      setContent(null);
      return;
    }
    loader().then((mod) => setContent(mod));
  }, [entry, locale]);

  if (!entry) {
    return (
      <div className="flex-1 w-full max-w-2xl pt-8">
        <p className="text-white/40 text-sm">Post not found.</p>
        <Link to="/blog" className="text-mystic-gold/80 text-sm hover:underline">
          {t("blog.backToList")}
        </Link>
      </div>
    );
  }

  const meta = locale === "zh" ? entry.zh : entry.en;
  const dateStr = new Date(entry.date).toLocaleDateString(
    locale === "zh" ? "zh-CN" : "en-US",
    { year: "numeric", month: "long", day: "numeric" }
  );

  return (
    <div className="flex-1 w-full max-w-2xl pt-8">
      <Link
        to="/blog"
        className="text-white/40 text-sm hover:text-white/70 transition-colors"
      >
        {t("blog.backToList")}
      </Link>

      <article className="mt-4">
        <h1 className="font-serif text-2xl text-white mb-2">{meta.title}</h1>
        <time className="text-white/30 text-sm">
          {t("blog.publishedOn")} {dateStr}
        </time>

        <div className="mt-8 bg-white/[0.03] border border-white/10 rounded-xl p-6 sm:p-8">
          {content === null ? (
            <p className="text-white/40 text-sm">Loading...</p>
          ) : (
            <div className="prose prose-invert prose-sm max-w-none
              prose-headings:text-white/90 prose-headings:font-serif prose-headings:mt-6 prose-headings:mb-3
              prose-p:text-white/60 prose-p:leading-relaxed
              prose-a:text-mystic-gold/80 prose-a:no-underline hover:prose-a:text-mystic-gold
              prose-strong:text-white/80
              prose-li:text-white/60
              prose-ul:my-3 prose-li:my-1
              [&_h1]:text-xl [&_h2]:text-lg [&_h2]:border-b [&_h2]:border-white/10 [&_h2]:pb-2
              [&_h3]:text-base [&_h3]:text-white/80">
              <ReactMarkdown>{content}</ReactMarkdown>
            </div>
          )}
        </div>
      </article>
    </div>
  );
}
```

- [ ] **Step 2: Verify TypeScript compiles**

Run: `npx tsc -b --noEmit`
Expected: No errors

- [ ] **Step 3: Commit**

```bash
git add src/pages/BlogPostPage.tsx
git commit -m "feat: add Blog post page with markdown rendering"
```

---

### Task 8: Register new routes in App.tsx

**Files:**
- Modify: `src/App.tsx`

**Interfaces:**
- Consumes: TosPage, PrivacyPage, BlogListPage, BlogPostPage

- [ ] **Step 1: Add imports and routes to App.tsx**

Add import lines after existing page imports:
```typescript
import TosPage from "./pages/TosPage";
import PrivacyPage from "./pages/PrivacyPage";
import BlogListPage from "./pages/BlogListPage";
import BlogPostPage from "./pages/BlogPostPage";
```

Add route entries after existing `<Route path="/result" ... />`:
```typescript
<Route path="/tos" element={<TosPage />} />
<Route path="/privacy" element={<PrivacyPage />} />
<Route path="/blog" element={<BlogListPage />} />
<Route path="/blog/:slug" element={<BlogPostPage />} />
```

Full App.tsx:
```typescript
import { HashRouter, Routes, Route } from "react-router-dom";
import { ReadingProvider } from "./context/ReadingContext";
import { LocaleProvider } from "./context/LocaleContext";
import { ModeProvider } from "./context/ModeContext";
import Layout from "./components/Layout";
import HomePage from "./pages/HomePage";
import ResultPage from "./pages/ResultPage";
import TosPage from "./pages/TosPage";
import PrivacyPage from "./pages/PrivacyPage";
import BlogListPage from "./pages/BlogListPage";
import BlogPostPage from "./pages/BlogPostPage";

export default function App() {
  return (
    <HashRouter>
      <LocaleProvider>
        <ModeProvider>
          <ReadingProvider>
            <Layout>
              <Routes>
                <Route path="/" element={<HomePage />} />
                <Route path="/result" element={<ResultPage />} />
                <Route path="/tos" element={<TosPage />} />
                <Route path="/privacy" element={<PrivacyPage />} />
                <Route path="/blog" element={<BlogListPage />} />
                <Route path="/blog/:slug" element={<BlogPostPage />} />
              </Routes>
            </Layout>
          </ReadingProvider>
        </ModeProvider>
      </LocaleProvider>
    </HashRouter>
  );
}
```

- [ ] **Step 2: Verify TypeScript compiles**

Run: `npx tsc -b --noEmit`
Expected: No errors

- [ ] **Step 3: Commit**

```bash
git add src/App.tsx
git commit -m "feat: add ToS, Privacy, and Blog routes"
```

---

### Task 9: Build and verify

- [ ] **Step 1: Run production build**

Run: `npm run build`
Expected: Build succeeds with no errors.

- [ ] **Step 2: Start dev server and do quick smoke test**

Run: `npm run dev` in background
Then visit:
- `http://localhost:5173/#/tos` — should show Terms of Service page
- `http://localhost:5173/#/privacy` — should show Privacy Policy page
- `http://localhost:5173/#/blog` — should show blog list with Getting Started article
- `http://localhost:5173/#/blog/getting-started` — should show blog post
- Footer should be visible on all pages with working links

- [ ] **Step 3: Switch language to English, verify all pages render in English**

- [ ] **Step 4: No build, no errors — final commit if any fixes were needed**
